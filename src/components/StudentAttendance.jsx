import React from "react";
import { Container, Typography } from "@mui/material";

const StudentAttendance = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Student Attendance Page</Typography>
    </Container>
  );
};

export default StudentAttendance;
